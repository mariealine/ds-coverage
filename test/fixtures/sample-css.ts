// Sample file with CSS hardcoded values for testing
const styles = {
  color: "#ff0000",
  fontSize: "14px",
  borderRadius: "8px",
  backgroundColor: "rgba(0, 0, 0, 0.5)",
};

export function getStyles() {
  return styles;
}
